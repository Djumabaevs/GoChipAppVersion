<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".login.newLogin.ChooseLoginActivity">

        <Button
            android:id="@+id/loginEmailAuth"
            android:layout_width="217dp"
            android:layout_height="84dp"
            android:layout_marginStart="97dp"
            android:layout_marginEnd="97dp"
            android:layout_marginBottom="249dp"
            android:text="Email Auth"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <Button
            android:id="@+id/loginKeycloak"
            android:layout_width="217dp"
            android:layout_height="84dp"
            android:layout_marginStart="97dp"
            android:layout_marginEnd="97dp"
            android:layout_marginBottom="73dp"
            android:text="Keycloak Auth"
            app:layout_constraintBottom_toTopOf="@+id/loginEmailAuth"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>